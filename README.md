This map is using Mapbox Direction API to display the current position if provided or Cairo, Egypt by default. A path is generated on the map between the start and the end points when clicking on the map, also instructions for navigations and a trip duration in minutes.
